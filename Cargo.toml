[package]
name = "ai_gateaway"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-native-tls = "0.3"
native-tls = "0.2"

serde = { version = "1", features = ["derive"] }
serde_json = "1"